import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
// import { BusStopsPage } from './bus-stops';

@NgModule({
  declarations: [
    // BusStopsPage,
  ],
  imports: [
    // IonicPageModule.forChild(BusStopsPage),
  ],
})
export class BusStopsPageModule {}
